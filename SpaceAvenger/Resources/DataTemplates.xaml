<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:userVM="clr-namespace:SpaceAvenger.ViewModels.UserProfile"
    xmlns:sfVM="clr-namespace:SpaceAvenger.ViewModels.SubFactionCard"
    xmlns:res="clr-namespace:SpaceAvenger.Resources.Strings"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    >
    <Style x:Key="UserProfText">
        <Style.Setters>
            <Setter Property="ItemsControl.FontSize" Value="20"/>
            <Setter Property="ItemsControl.Foreground" Value="White"/>
        </Style.Setters>
    </Style>

    <DataTemplate DataType="{x:Type userVM:UserProfileVM}">
        <Border x:Name="brd" CornerRadius="10" Style="{DynamicResource UserProfText}" 
            BorderThickness="2" Margin="10">
            <Border.BorderBrush>
                <LinearGradientBrush>
                    <LinearGradientBrush.RelativeTransform>
                        <RotateTransform x:Name="rtr" Angle="0"/>
                    </LinearGradientBrush.RelativeTransform>

                    <GradientStop x:Name="brd1gs" Color="{StaticResource brdButColor1}" Offset="1"/>
                    <GradientStop x:Name="brd2gs" Color="{StaticResource brdButColor2}" Offset="0"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Number, UpdateSourceTrigger=PropertyChanged, StringFormat=№{0})}"
                       Margin="10, 40, 0,0" FontSize="30"/>
                <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                    <Image x:Name="userVm_Image" Width="150" Height="150" Margin="40,0"/>
                    <ToggleButton Style="{DynamicResource ToggleButton}"
                              Content="{Binding MaleFemale, UpdateSourceTrigger=PropertyChanged
                    ,Converter={StaticResource BoolToFemaleInvertConverter}
                    }" IsChecked="{Binding MaleFemale, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    x:Name="chooseSex_btn"
                              />
                </StackPanel>
                <StackPanel Orientation="Vertical" Margin="30,0,0,0">
                    <TextBlock Text="Commander Info:" FontSize="22" Margin="0,0,0,10"/>
                    <TextBlock 
                    Text="{Binding EnlistedDate, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, StringFormat=Enlisted: {0:yyyy.MM.dd HH:MM}, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='Not yet'}" 
                    Margin="0,0,0,10"/>
                    <TextBox Style="{DynamicResource TextBox}" 
                         Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                         Margin="0,0,0,10" 
                         IsReadOnly="{Binding Confirmed, UpdateSourceTrigger=PropertyChanged}"
                         />
                    <TextBlock Text="{Binding RankName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, StringFormat= Rank: {0}}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding MissionsCount, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, StringFormat='Missions Completed: {0}'}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Points, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, StringFormat=Experience: {0}}"/>
                </StackPanel>
                <StackPanel Orientation="Vertical"
                        x:Name="confirm_block" Margin="40,10,20,0">
                    <TextBlock Text="Check your personal info commander. And if all is correct, press Confirm Profile button bellow. "
                           TextWrapping="WrapWithOverflow" Width="300"
                           FontSize="20" Foreground="White" Margin="10"/>
                    <Button Content="Confirm Profile" Style="{DynamicResource Button}"
                        Command="{Binding OnConfirmButtonPressed}"
                        />

                </StackPanel>

                <StackPanel Orientation="Vertical" x:Name="profile_confirmed_block"
                        Margin="50,0,50,0">
                    <Label Content="Profile Confirmed."
                FontSize="25" Foreground="#FF3CB435"
                Margin="{Binding ElementName=confirm_block, Path=Margin, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                    <Button Content="Select" Style="{DynamicResource Button}" Margin="0,20"
                        Command="{Binding OnSelectButtonPressed}"/>
                </StackPanel>

            </StackPanel>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Confirmed, UpdateSourceTrigger=PropertyChanged}" Value="True">
                <DataTrigger.Setters>
                    <Setter Property="Visibility" TargetName="chooseSex_btn"
                        Value="Collapsed"/>
                    <Setter Property="Visibility" TargetName="confirm_block"
                        Value="Collapsed"/>
                    <Setter Property="Visibility" TargetName="profile_confirmed_block"
                        Value="Visible"/>
                </DataTrigger.Setters>
            </DataTrigger>

            <DataTrigger Binding="{Binding Confirmed, UpdateSourceTrigger=PropertyChanged}" Value="False">
                <DataTrigger.Setters>
                    <Setter Property="Visibility" TargetName="profile_confirmed_block"
                        Value="Collapsed"/>
                    <Setter Property="Visibility" TargetName="chooseSex_btn"
                        Value="Visible"/>
                    <Setter Property="Visibility" TargetName="confirm_block"
                        Value="Visible"/>
                </DataTrigger.Setters>
            </DataTrigger>
            <!--Show male-->
            <DataTrigger Binding="{Binding MaleFemale, 
            UpdateSourceTrigger=PropertyChanged}" Value="True">
                <DataTrigger.Setters>
                    <Setter TargetName="userVm_Image" Property="Source" 
                        Value="{StaticResource HumanCM}"/>
                </DataTrigger.Setters>
            </DataTrigger>
            <!--Show female-->
            <DataTrigger Binding="{Binding MaleFemale, 
            UpdateSourceTrigger=PropertyChanged}" Value="False">
                <DataTrigger.Setters>
                    <Setter TargetName="userVm_Image" Property="Source" 
                        Value="{StaticResource HumanCF}"/>
                </DataTrigger.Setters>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate DataType="{x:Type sfVM:SubFactionCardVM}" x:Key="SubFactionCardDatatemplate">
        <Border Margin="20" Style="{StaticResource BorderStype}">
            <StackPanel Orientation="Vertical">
                <TextBlock Style="{StaticResource textBlockHeaders}"
                           Text="{Binding Title}"
                           HorizontalAlignment="Center"/>
                <Rectangle Height="200" Width="200" Margin="0,20">
                    <Rectangle.Fill>
                        <ImageBrush Stretch="Uniform" ImageSource="{Binding Icon, UpdateSourceTrigger=PropertyChanged}"/>
                    </Rectangle.Fill>
                </Rectangle>
                <TextBlock Margin="10" Style="{StaticResource textBlockHeaders}"
                           Text="{Binding ShortDesc}"
                           TextWrapping="WrapWithOverflow"
                           Width="400"/>
                <StackPanel Orientation="Vertical" 
                            Visibility="{Binding MoreLess,UpdateSourceTrigger=PropertyChanged, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=False}">
                    <Rectangle Fill="White" Height="3" Margin="5"/>
                    <TextBlock Text="{Binding Description}" 
                           Style="{StaticResource textBlockHeaders}"
                           TextWrapping="WrapWithOverflow"
                           Width="400"/>
                </StackPanel>
                <Rectangle Fill="White" Height="3" Margin="5"/>
                <StackPanel Orientation="Horizontal" Margin="15">
                    <Button Content="{Binding MoreButtonName, UpdateSourceTrigger=PropertyChanged}" 
                            Margin="0,0,20,0" Style="{StaticResource Button}" 
                            Command="{Binding OnReadMoreButtonPressed}"/>
                    <Button Content="{x:Static res:UIStrings.SelectButton}" 
                            Style="{StaticResource Button}"
                            Command="{Binding OnSelectButtonPressed}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </DataTemplate>

</ResourceDictionary>